awk '
NR==1 { prev=$0; count=1; next }

{
    if ($0 == prev) {
        count++
    } else {
        if (count == 1) print prev
        prev = $0
        count = 1
    }
}

END {
    if (count == 1) print prev
}'